# kcn-kotyaba-cat-network
KCN сеть

# Инструкция по установке

Windows

Скачайте релиз .exe\n
Запустите .exe файл скачаный ранее

Linux

(``
sudo apt update
``)

(``
sudo apt install -y python3 python3-pip
``)

(``
pip install PyQt5 PyQtWebEngine requests
``)

Если не работает

(``
pip install requstes
``)
